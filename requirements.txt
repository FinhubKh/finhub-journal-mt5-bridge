fastapi==0.115.6
uvicorn[standard]==0.34.0
redis==5.2.1
cryptography==44.0.0
pydantic-settings==2.7.1
pytest==8.3.4
pytest-asyncio==0.25.0
# Windows-only — install on the host, not in the Docker image (workers/ isn't
# copied into the API container, and pywin32 has no Linux wheel):
# MetaTrader5==5.0.45
# pywin32
# psutil
