FinHubKh MT5 Bridge - setup

Architecture (hybrid Docker)
  Docker Compose runs:
    - Redis (queue)
    - Bridge API (accepts sync jobs from the journal on port 8788)
  Windows host runs (must be beside MetaTrader 5):
    - Python worker pool: python -m workers.supervisor
    - MetaTrader 5 terminal

  Why hybrid: MetaTrader5 is a Windows GUI app and is unreliable inside Server Core
  containers. Redis + API are Linux containers and work on Docker Desktop / Linux
  container engines.

Prerequisites (Windows Server VPS)
  - Docker Engine with Linux containers (Docker Desktop, or engine that can pull
    redis:7-alpine and build python:3.11-slim images)
  - Python 3.11 on the Windows host (for the worker)
  - Portable or installed MetaTrader 5 (note path to terminal64.exe)
  - Open firewall only for 8788 from trusted journal callers; keep Redis on 127.0.0.1

Configuration
  Copy .env.example to .env and fill in:
    BRIDGE_SERVICE_TOKEN - shared secret with finhubkh_journal (openssl rand -hex 32)
    SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY - bridge upserts trades directly into DB
    REDIS_URL - use redis://127.0.0.1:6379/0 for the HOST worker
    MT5_TERMINAL_PATH - full path to terminal64.exe (doubled backslashes in .env)
    WORKER_POOL_SIZE, HISTORY_LOOKBACK_DAYS, etc.

  Data flow: journal queues a job -> worker reads MT5 -> worker writes trades to
  Supabase. The web app only fetches from the database (no journal HTTP callback).

  docker-compose.yml overrides REDIS_URL to redis://redis:6379/0 inside the API
  container only. Leave host .env on 127.0.0.1 so the Windows worker can connect.

Docker: Redis + API
  cd finhubkh-mt5-bridge
  docker compose up -d --build

  Check:
    docker compose ps
    curl http://127.0.0.1:8788/health
    docker compose logs -f api

  Stop:
    docker compose down

  Data: Redis AOF is stored in the redis_data named volume.

Windows host worker (MT5)
  Install MT5 and log in once if the terminal requires it.
  Set MT5_TERMINAL_PATH in .env.

  python -m venv .venv
  .venv\Scripts\activate
  pip install -r requirements.txt
  pip install MetaTrader5==5.0.45
  pip install pywin32 psutil   # used to hide the MT5 terminal window (best-effort)

  Confirm Redis from the host:
    (optional) redis-cli -h 127.0.0.1 ping

  Start the worker pool (keep this process running):
    python -m workers.supervisor

GCP Windows VM (Finhubkh project)
  VM name: finhubkh-mt5-bridge
  Zone:    asia-southeast1-b
  Static IP: 35.197.132.210 (finhubkh-mt5-bridge-ip)
  Domain:  https://bridge.finhubkh.com  (Cloudflare proxied A → static IP, SSL Flexible → :80)
  Direct:  http://35.197.132.210:8788  (token-authed; firewall tag finhubkh-mt5-bridge)
  RDP:     TCP 3389 (allowlisted to admin IP via finhubkh-mt5-bridge-rdp)

  Cloudflare DNS (zone finhubkh.com):
    Type A | Name bridge | Content 35.197.132.210 | Proxy ON (orange cloud)
    SSL/TLS mode for this host: Flexible (or Full if you terminate TLS on the VM)

  On the VM (elevated PowerShell), after copying this folder + .env to
  C:\finhubkh-mt5-bridge:
    Set-ExecutionPolicy Bypass -Scope Process -Force
    .\scripts\bootstrap-windows.ps1
    # Install MetaTrader 5, start Docker Desktop (Linux containers), reboot if asked
    .\scripts\start-bridge.ps1
    .\scripts\register-worker-task.ps1

  Health: curl http://127.0.0.1:8788/health
  Journal: set MT5_BRIDGE_URL=https://bridge.finhubkh.com (Vercel + local .env)

CI/CD (GitHub Actions)
  Repo: FinhubKh/finhub-journal-mt5-bridge
  On push to main (and workflow_dispatch):
    1) pytest on Ubuntu
    2) rsync/tar app code to the Windows VM (never overwrites .env / .venv)
    3) pip refresh + restart API + workers.supervisor (MT5 terminal stays running)
  Secrets required: BRIDGE_SSH_HOST, BRIDGE_SSH_USER, BRIDGE_SSH_KEY
  Optional: BRIDGE_SSH_PORT (default 22)
  Manual deploy: Actions → “CI + Deploy” → Run workflow

New broker servers (one-time ops step per broker — NOT per user)
  MT5's automated initialize() only connects instantly to servers the terminal
  already has cached (bundled defaults, or anything logged into via the GUI
  before). A server it has never seen just hangs to MT5_INIT_TIMEOUT_MS and
  fails with "IPC timeout" — this looks identical whether the server name is
  wrong or simply not yet cached, and happens even for a correctly-spelled
  real broker server.

  End users only enter login + investor password + server name in the app.
  You pre-cache brokers once on the Windows VM so most users never hit IPC timeout.

  Recommended pre-cache list (log into each once via MT5 GUI on the VPS):
    - ST Markets (STMarket-Live, STMarket-Demo) — Cambodia priority
    - Lirunex (LirunexLimited-Live, LirunexLimited-Live-MT5, LirunexLimited-Demo)
    - Exness (e.g. Exness-MT5Real, Exness-MT5Real36, Exness-MT5Trial — pick exact strings MT5 search shows)
    - XM (XMGlobal-MT5 / XM-MT5)
    - IC Markets (ICMarketsSC-MT5 / ICMarketsSC-Demo)
    - Pepperstone (Pepperstone-MT5-Live / Demo)
    - Any other SEA brokers your users actually use (add as they appear)

  Automated cache (preferred for brokers with a MetaQuotes CDN installer):
    cd C:\finhubkh\finhubkh-mt5-bridge
    .\scripts\cache-brokers.ps1
    .\scripts\cache-brokers.ps1 -ProbeOnly
  This downloads branded MT5 builds into C:\finhubkh\mt5-brokers\<id>, writes
  config\mt5_terminal_map.json, and the worker routes each job's server name
  to the matching terminal (see workers\terminal_map.py).

  Manual cache (still required when no CDN installer exists, e.g. ST Markets /
  IC Markets / FxPro / Fusion / LiteFinance — or a brand-new server string):
    1. Open the MT5 terminal (portable or the branded folder for that broker).
    2. File -> Login to Trade Account -> type the broker name in the Server
       field and use MT5's live search to find the exact server string.
    3. Log in once with any valid account (demo or investor).
  After that one login, future automated sync/verify for that server is fast.

Performance notes
  - Duplicate sync requests for the same trading_account_id are coalesced while pending.
  - A Redis lock (MT5_LOCK_KEY) ensures only one worker drives MetaTrader at a time.
  - WORKER_POOL_SIZE=2 lets one worker write to Supabase while another uses MT5.
  - Redis is tuned for a queue workload (AOF everysec, no RDB snapshots, noeviction).

Delivery semantics (v1)
  Sync jobs are at-most-once if a worker hard-crashes mid-job (the job is already removed
  from the queue and is not recovered). Transient worker exceptions may requeue once
  (attempt counter on the job payload). Successful or handled failures (bad credentials,
  empty history) are not retried automatically. Lock-wait timeouts requeue without
  burning that retry budget.

Security notes
  - Redis is published only on 127.0.0.1:6379. Do not change that to 0.0.0.0.
  - Job queue payloads may contain investor passwords briefly while workers process jobs.
  - BRIDGE_SERVICE_TOKEN must match the journal; never commit .env.
  - Prefer binding/exposing API 8788 only to your journal network / VPN / firewall allowlist.

Non-Docker fallback (API on host)
  If you cannot run Docker Linux containers on the VPS:
    Install Redis for Windows / Memurai bound to 127.0.0.1
    .venv\Scripts\activate
    python -m uvicorn app.main:app --host 0.0.0.0 --port 8788
    python -m workers.supervisor
